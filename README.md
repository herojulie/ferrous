# ferrous

Personal Rust project for two goals:

1. Learning Rust through practical implementation.
2. Adding work-related helper features that should run locally for security.

## Run locally

- One-click on macOS: double-click `run.command` in Finder.
- Terminal option: run `cargo run` in this directory.
- The app now opens `http://localhost:8000` automatically when it starts.
